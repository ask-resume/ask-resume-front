const convertStringToLowerCase = (string: string) => string.toLowerCase();
const convertStringToUpperCase = (string: string) => string.toUpperCase();

export { convertStringToLowerCase, convertStringToUpperCase };
